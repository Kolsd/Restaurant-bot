# Secrets — never commit these
.env
.env.*
!.env.example
app/.env

# Claude Code local settings (may contain API keys in allowed commands)
.claude/settings.local.json

# Python
__pycache__/
*.py[cod]
*.pyo
.venv/
venv/
*.egg-info/
.pytest_cache/

# Alembic
alembic/versions/*.pyc

# AI sim reports (real E2E test runs — generated at runtime)
tests/ai_sim/reports/
