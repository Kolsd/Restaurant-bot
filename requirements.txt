fastapi==0.115.14
uvicorn[standard]==0.34.3
anthropic==0.50.0
asyncpg==0.30.0
python-multipart==0.0.20
pydantic==2.10.6
aiofiles==24.1.0
pypdf==4.3.1
passlib==1.7.4
bcrypt==3.2.2
httpx==0.28.1
python-dotenv==1.1.0
# ── Migrations ────────────────────────────────────────────────────────
alembic==1.14.0
psycopg2-binary==2.9.9
tzdata==2025.3
webauthn>=2.0.0,<3.0.0
structlog>=24.1.0
redis>=5.0
cloudinary>=1.40,<2.0          # Catálogo visual v2 — Fase 1. MVP usa free tier 25 GB.
sentry-sdk[fastapi]>=2.18,<3.0 # Error tracking + tracing. Activado vía SENTRY_DSN env var.
# ── Testing ───────────────────────────────────────────────────────────
pytest>=8.0
pytest-asyncio>=0.24
pytest-xdist>=3.6         # Optional parallel runner: `pytest -n auto` (~21% speedup on full suite)
asgi-lifespan>=2.1        # E2E tests: ASGI lifespan management for httpx
